
  Stock Market Simulator

  This is a code bundle for Stock Market Simulator.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
